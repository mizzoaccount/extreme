/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos", "i.pinimg.com","res.cloudinary.com", "randomuser.me", "via.placeholder.com"], // Add both domains here
  },
};

export default nextConfig;
